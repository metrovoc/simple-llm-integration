pluginManagement {
	repositories {
		gradlePluginPortal()
		mavenCentral()
		maven { url = 'https://maven.neoforged.net/releases' }
	}
}

rootProject.name = 'simple-llm'

include 'common'
include 'implementations:neoforge'
